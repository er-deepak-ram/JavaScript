"use strict"; // Treat all JS code as newer version

// alert("Hello, World!") // We are using nodejs, not browser

let name = "Deeppak"
let age = 25
let isLoggedIn = false

// Premitives
// number => Max range of Number datatype is 2 to power 53
// bigint => Can hold bigger numeric value than number
// String => "" and '' both works but personal prefference -> ""
// boolean => true/false. Only these two values
// null => null is a standalone value (Representation of an empty value)
// undefined => uninitialized (Represents that the variable is not yet initialized)
// symbol => unique (Most implementation in React, for uniqueness of diff components)

// Object
console.log(typeof "Deeppak"); // string
console.log(typeof age); // number
console.log(typeof null); // Some say that it's bug in JS, it shows object
console.log(typeof undefined); // undefined



